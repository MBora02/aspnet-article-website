<nav class="navbar navbar-expand-lg navbar-dark ftco_navbar bg-dark ftco-navbar-light" id="ftco-navbar">
    <div class="container">
        <a class="navbar-brand" href="/Default/Index">Makale<span>Sitesi</span></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#ftco-nav" aria-controls="ftco-nav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="oi oi-menu"></span> Menu
        </button>

        <div class="collapse navbar-collapse" id="ftco-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item"><a href="/Default/Index" class="nav-link">Ana Sayfa</a></li>
                <li class="nav-item"><a href="/Article/Index" class="nav-link">Makaleler</a></li>

                @if (User.Identity.IsAuthenticated)
                {
                    if (User.IsInRole("Admin"))
                    {
                        <li class="nav-item"><a href="/Admin/AdminProfile/Index" class="nav-link">Admin Paneli</a></li>
                    }
                    else if (User.IsInRole("Instructor") || User.IsInRole("Student"))
                    {
                        <li class="nav-item"><a href="/UserProfile/Index" class="nav-link">Profil</a></li>
                    }

                    <li class="nav-item">
                        <form method="post" asp-controller="Login" asp-action="Logout" class="d-inline">
                            @Html.AntiForgeryToken()
                            <button type="submit" class="btn btn-link nav-link" style="cursor:pointer; padding:0; border:none; background:none;">
                                Çıkış Yap
                            </button>
                        </form>
                    </li>
                }
                else
                {
                    <li class="nav-item"><a href="/Login/Index" class="nav-link">Giriş</a></li>
                }
            </ul>
        </div>
    </div>
</nav>

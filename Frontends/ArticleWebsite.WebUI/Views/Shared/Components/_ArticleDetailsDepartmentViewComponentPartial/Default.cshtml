@model List<ResultDepartmentDto>
<div class="sidebar-box ftco-animate">
    <div class="categories">
        <h3>Bölümler</h3>
        @foreach (var item in Model)
        {
            <li><a href="#">@item.Name <span>(0)</span></a></li>
        }
    </div>
</div>